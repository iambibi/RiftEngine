name: RiftEngine
version: ${version}
main: fr.openmc.riftengine.core.RiftPlugin
description: Plugin convert Java pack to Bedrock pack
api-version: ${minecraftVersion}
# bootstrapper: fr.openmc.riftengine.core.RiftBootstrap

dependencies:
  server:
    OpenMC:
      load: BEFORE
      required: true
    ItemsAdder:
      load: BEFORE
      required: true
    Geyser-Spigot:
      load: BEFORE
      required: true